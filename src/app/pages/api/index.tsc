import React from 'react';
import Scanner from '../components/Scanner'; // Adjust the import path based on your project structure

const HomePage = () => {
  return (
    <div>
      <Scanner />
    </div>
  );
};

export default HomePage;
